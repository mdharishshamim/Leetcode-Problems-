vector<int> getConcatenation(vector<int>& nums) {
        vector<int> copy;
        for(int j = 0;j<2;j++){
            for(int i = 0; i < nums.size(); i++){
                copy.push_back(nums[i]);
            }
        }
        return copy;
    }
